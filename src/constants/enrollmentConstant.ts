export const ENROLLMENT_WATCH_PERCENTAGE_MIN = 0;
export const ENROLLMENT_WATCH_PERCENTAGE_MAX = 100;

// A student rarely watches to the exact last frame (outro, credits, etc.),
// so completion is granted once this much of the course has been reached.
export const ENROLLMENT_WATCH_COMPLETION_THRESHOLD_PERCENTAGE = 95;
